# Runtime (the app)
streamlit>=1.40
google-genai>=0.3
pandas>=2.0
numpy>=1.26
pydantic>=2.0
scikit-surprise>=1.1.4   # runtime: app loads & predicts with the pickled CF model

# Build-time only (offline scripts)
Faker>=30.0

# Dev
pytest>=8.0
